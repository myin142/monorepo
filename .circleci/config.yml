version: 2.1
orbs:
    node: circleci/node@3.0.0
jobs:
    deploy:
        executor: node/default
        steps:
            - checkout
            - node/install-packages
            - run: npm run build
            - run: ./scripts/gh-deploy $GH_TOKEN
    test:
        executor: node/default
        steps:
            - checkout
            - node/install-packages
            - run: npm test
    integration:
        executor: node/default
        steps:
            - checkout
            - node/install-packages
            - run: npm run e2e

workflows:
    build-and-test:
        jobs:
            - deploy:
                  requires:
                      - test
                      - integration
            - test
            - integration:
                  filters:
                      branches:
                          only:
                              - master
